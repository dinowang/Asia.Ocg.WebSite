import { createAction } from 'redux-actions';
export const searchCard = createAction('search card');
export const changeBtnType = createAction('change btntype');
export const changeDate = createAction('change date');
